# =============================================================================
# Agent System — Environment Configuration
# =============================================================================
# Most users don't need this file! Running `npm run dev:ui` will prompt you
# interactively for the LLM provider and API key, and the embedded database
# requires no configuration.
#
# Copy this to .env only if you want to override defaults.

# ── LLM Provider ────────────────────────────────────────────────────────────
# Set via interactive prompt on first run, or manually here.
# LLM_PROVIDER=gemini            # gemini, anthropic, openai, ollama, openrouter
# LLM_API_KEY=                   # API key for your provider
# GEMINI_API_KEY=                # Alternative: Gemini-specific key
# OLLAMA_BASE_URL=http://127.0.0.1:11434 # Optional: override Ollama host/port
# OLLAMA_ROUTER_MODEL=llama3.3         # Optional: router model when using Ollama
# OLLAMA_AGENT_MODEL=qwen3:32b         # Optional: agent model when using Ollama
# OLLAMA_WORK_MODEL=qwen3:32b          # Optional: work model when using Ollama
# OLLAMA_EMBEDDING_MODEL=nomic-embed-text # Optional: embedding model when using Ollama
# OPENAI_BASE_URL=https://api.openai.com/v1 # Optional: override base URL for OpenAI-compatible backends
# OPENAI_ROUTER_MODEL=gpt-4.1-mini     # Optional: router model when using OpenAI
# OPENAI_AGENT_MODEL=gpt-4.1           # Optional: agent model when using OpenAI
# OPENAI_WORK_MODEL=o3                 # Optional: work model when using OpenAI
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small # Optional: embedding model when using OpenAI

# ── Database ────────────────────────────────────────────────────────────────
# Default: embedded PGlite (zero-config, stored in ./data/pglite/)
# Set DATABASE_URL to use external PostgreSQL instead:
# DATABASE_URL=postgresql://agents:agents@localhost:5432/agents

# ── Execution Backend ───────────────────────────────────────────────────────
# What runs approved tickets. Default: noop (track only)
# EXECUTION_BACKEND=claude-code  # claude-code, gemini-cli, codex-cli, openclaw, permaship

# ── Optional ────────────────────────────────────────────────────────────────
# LOCAL_UI_PORT=3000
# LOG_LEVEL=info
# ADAPTER_PROFILE=default        # default or permaship
